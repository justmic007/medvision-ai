[pytest]
# Note: torch emits harmless SourceChangeWarnings when unpickling the
# torchxrayvision weights (pickled under a slightly different torch build).
# They fire deep in torch.serialization during model load and resist pytest
# filtering; they're cosmetic — inference is correct. Not worth suppressing.
filterwarnings =
    ignore::DeprecationWarning
