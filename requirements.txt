# MedVision AI — Phase 0 dependencies
# Pinned with ~= (compatible release): patch/minor updates allowed, majors held.
# The heavier imaging stack (torch, torchxrayvision, monai, mlflow) is added in
# Phase 1/2 to keep the Phase 0 image small and fast to build.

fastapi~=0.141.0
uvicorn[standard]~=0.52.0
pydantic-settings~=2.15.0

# --- dev / test ---
pytest~=8.4.0
httpx~=0.28.0        # required by fastapi.testclient

# --- Phase 1: imaging & preprocessing ---
# Pinned to the Intel-macOS ceiling: torch tops out at 2.2.2 on x86-64 macOS
# (no Intel wheels after 2.2.x). torchvision 0.17.2 is its version-locked partner.
# See DECISIONS.md D-13.
torch==2.2.2
torchvision==0.17.2
torchxrayvision==1.5.4
pydicom~=3.0
pillow~=11.0
numpy~=1.26

# --- Phase 2: experiment tracking ---
mlflow~=2.17
